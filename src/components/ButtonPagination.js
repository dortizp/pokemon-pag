import styled from "@emotion/styled";

const ButtonPagination = () => {
  return (
    <div>
      <button>Prev</button>
      <button>Next</button>
    </div>
  );
};

export default ButtonPagination;
